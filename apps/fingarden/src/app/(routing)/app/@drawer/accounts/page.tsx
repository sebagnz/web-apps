/**
 * This page is necessary to catch all client side navigations back to /app/accounts.
 * If this component didn't exist, attempts to close the drawer with a client side navigation to /app/accounts would have no effect.
 */
export default function Page() {
  return null
}
